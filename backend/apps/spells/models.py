# The spells app has been replaced by `apps.quests`. This module is kept empty
# so the app can remain in INSTALLED_APPS until the legacy migration history
# can be safely pruned in a future release.
